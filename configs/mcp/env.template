# ============================================================================
# MCP Server Environment Variables Template
# ============================================================================
#
# Copy this file and configure your API keys:
#   cp ~/.config/mcp-env.template ~/.config/mcp-env
#   nvim ~/.config/mcp-env
#   source ~/.config/mcp-env
#
# Or add exports to ~/.zshrc.local for persistence
# ============================================================================

# --- GitHub Integration ---
# Required for GitHub MCP server (issues, PRs, repos)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, read:org, read:user
export GITHUB_PERSONAL_ACCESS_TOKEN=""

# --- Search APIs ---
# Brave Search API (web search capability)
# Get key at: https://brave.com/search/api/
export BRAVE_API_KEY=""

# Figma Personal Access Token (design-to-code workflows)
# Get at: https://www.figma.com/developers/api#access-tokens
export FIGMA_PERSONAL_ACCESS_TOKEN=""

# --- Database Connections ---
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
export POSTGRES_CONNECTION_STRING=""

# SQLite database path (defaults to ./database.db if not set)
export SQLITE_DB_PATH=""

# --- AI Provider API Keys ---
# These are used by the AI CLI tools themselves

# Anthropic API Key (for Claude Code if not using OAuth)
# Get at: https://console.anthropic.com/
export ANTHROPIC_API_KEY=""

# Google AI API Key (for Gemini CLI)
# Get at: https://aistudio.google.com/apikey
export GEMINI_API_KEY=""

# OpenAI API Key (optional, for OpenCode with OpenAI provider)
# Get at: https://platform.openai.com/api-keys
export OPENAI_API_KEY=""

# --- Browser Automation (Playwright) ---
# No API key required! Just install browsers once:
#   npx playwright install
# Then Playwright MCP server can control Chrome/Firefox/Safari
# See: docs/MCP_SERVERS.md for usage examples

# --- Memory Storage ---
# Directory for MCP memory persistence (auto-created)
export MCP_MEMORY_DIR="$HOME/.local/share/mcp"

# ============================================================================
# Quick Enable Guide
# ============================================================================
#
# To enable an MCP server, fill in the required values above, then:
#
# 1. Source this file:
#    source ~/.config/mcp-env
#
# 2. For Claude Code (~/.claude.json):
#    Change "disabled": true to "disabled": false
#
# 3. For OpenCode (~/.config/opencode/opencode.json):
#    Change "enabled": false to "enabled": true
#
# 4. Restart your AI CLI tool
#
# See docs/MCP_SERVERS.md for detailed setup instructions
# ============================================================================
