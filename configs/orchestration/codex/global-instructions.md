## Codex agents

Delegate explicitly when the work calls for it: the tiers are the custom agents in `~/.codex/agents/` (`implementor-complex`, `implementor-standard`, `executor`, `reviewer`), each with its own model and effort. Use the built-in `explorer` for read-only discovery. Delegates don't spawn agents of their own. The `delegation` and `verification` skills have the details.
